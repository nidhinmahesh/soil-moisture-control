package main

func Computation() {
	if temp > 73.4 && hum > 73.0 { //slow
		rpm = 1400.0

	} else if temp > 77.1 && hum > 77.0 { //med
		rpm = 2200.0

	} else if temp > 81.0 && hum > 81.0 { //high
		rpm = 2900.0

	}
}
